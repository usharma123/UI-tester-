import { httpRouter } from "convex/server";

const http = httpRouter();

// HTTP routes can be added here if needed
// WorkOS AuthKit handles auth through client-side JWT validation

export default http;

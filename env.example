# UI QA Agent - Environment Variables

# Required: OpenRouter API key for LLM
OPENROUTER_API_KEY=sk-or-v1-...

# LLM Configuration
# OPENROUTER_MODEL=anthropic/claude-sonnet-4.5
# GOALS=homepage UX + primary CTA + form validation + keyboard

# Test Limits
# MAX_STEPS=20              # Maximum total test steps
# MAX_PAGES=50              # Maximum pages to test
# STEPS_PER_PAGE=5          # Max steps per individual page
# PARALLEL_BROWSERS=5       # Concurrent browser instances (1-10)

# Timeouts (milliseconds)
# BROWSER_TIMEOUT=60000     # General browser timeout
# NAVIGATION_TIMEOUT=45000  # Page load timeout
# ACTION_TIMEOUT=15000      # Click/fill timeout

# Retry Behavior
# MAX_RETRIES=3             # Retry attempts before skipping
# RETRY_DELAY_MS=1000       # Initial retry delay (doubles each attempt)

# Features
# AUDITS_ENABLED=true       # Run accessibility/performance audits
# STRICT_MODE=false         # Fail on any error vs skip and continue
# CAPTURE_BEFORE_AFTER=true # Screenshot before and after actions

# Viewports (format: label:WIDTHxHEIGHT,...)
# VIEWPORTS=desktop:1365x768,tablet:820x1180,mobile:390x844

# Debug
# DEBUG=false

# UI QA Agent - Environment Variables

# Required: OpenRouter API key for LLM
OPENROUTER_API_KEY=sk-or-v1-...

# Optional: Default LLM model (defaults to google/gemini-2.5-flash)
# OPENROUTER_MODEL=google/gemini-2.5-flash

# Optional: Test configuration
# MAX_STEPS=20           # Maximum test steps per page
# MAX_PAGES=10           # Maximum pages to test
# PARALLEL_BROWSERS=3    # Number of parallel browser instances
# BROWSER_TIMEOUT=30000  # Browser command timeout in ms

# Optional: Default test goals
# GOALS=homepage UX + primary CTA + form validation + keyboard

# Optional: Enable debug output
# DEBUG=true
